app.name=University ERP Desktop

# Storage mode: "memory" (default) or "jdbc"
storage.mode=memory

# Auth database connection (used when storage.mode=jdbc)
auth.jdbcUrl=jdbc:mysql://localhost:3306/univ_auth_db
auth.username=erp_auth
auth.password=changeme
auth.maxPoolSize=5

# ERP database connection (used when storage.mode=jdbc)
erp.jdbcUrl=jdbc:mysql://localhost:3306/univ_erp_db
erp.username=erp_app
erp.password=changeme
erp.maxPoolSize=10

